{
  "meta": {
    "type": "settings-spec",
    "app": "Between Us",
    "version": "1.1.0",
    "description": "Design spec for user settings. This file is NOT imported at runtime. Actual settings are managed by Supabase-backed services plus cache-only device preferences.",
    "status": "design-document-only"
  },
  "implemented": {
    "partnerNames": {
      "engine": "NicknameEngine (services/PolishEngine.js)",
      "storage": "cache-only device preference + AppContext PARTNER_LABEL",
      "ui": "components/NicknameSettings.jsx in SettingsScreen",
      "fields": {
        "myNickname": "How the user wants to be called",
        "partnerNickname": "How their partner is called (synced to AppContext.partnerLabel on change)"
      }
    },
    "heatLevel": {
      "engine": "PremiumGatekeeper + ContentContext",
      "storage": "Supabase profile when available; cache-only fallback",
      "ui": "HeatLevelSettingsScreen + OnboardingScreen",
      "levels": ["Emotional Connection", "Flirty & Romantic", "Sensual", "Steamy", "Explicit"]
    },
    "tone": {
      "engine": "NicknameEngine (services/PolishEngine.js)",
      "storage": "cache-only device preference",
      "options": ["warm", "playful", "intimate", "minimal"],
      "ui": "components/NicknameSettings.jsx"
    },
    "appLock": {
      "engine": "AppContext + PrivacySecuritySettingsScreen",
      "storage": "cache-only app-lock enabled flag",
      "ui": "screens/PrivacySecuritySettingsScreen.js"
    },
    "relationshipSeason": {
      "engine": "RelationshipSeasons (services/PolishEngine.js)",
      "storage": "cache-only device preference",
      "ui": "components/SeasonSelector.jsx in SettingsScreen"
    },
    "softBoundaries": {
      "engine": "SoftBoundaries (services/PolishEngine.js)",
      "storage": "cache-only device preference",
      "ui": "components/SoftBoundariesPanel.jsx in SettingsScreen"
    }
  },
  "notImplemented": {
    "bodyPositive": {
      "originalSpec": "Include prompts that celebrate all body types and abilities",
      "reason": "No prompts in prompts.json have a bodyPositive metadata tag. Needs content tagging before filter can work.",
      "effort": "medium"
    },
    "savePreferences": {
      "originalSpec": "Remember My Settings",
      "reason": "All settings are always persisted. This toggle has no useful behavior.",
      "recommendation": "remove from spec"
    },
    "anonymousUsage": {
      "originalSpec": "Don't store any personal preference data",
      "reason": "The app uses limited analytics, diagnostics, and synced profile preferences. This toggle would need a narrower product definition before implementation.",
      "recommendation": "remove from spec"
    }
  },
  "onboardingFlow": {
    "note": "Actual onboarding is in screens/OnboardingScreen.js",
    "steps": ["welcome", "partnerNames", "heatLevel", "relationshipDuration", "tone"]
  }
}
